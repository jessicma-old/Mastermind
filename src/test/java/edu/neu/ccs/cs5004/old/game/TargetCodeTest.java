package edu.neu.ccs.cs5004.old.game;

/**
 * Created by jessicamalloy on 1/17/17.
 */
public class TargetCodeTest {
/*
    TargetCode dup;
    TargetCode noDup;

    @Before
    public void setUp() throws Exception {
        dup = new TargetCode();
        noDup = new TargetCode();
        noDup.clear();
        noDup.generateTargetNoDup();
    }

    @Test
    public void generateTarget() throws Exception {
        // length
        Assert.assertEquals(dup.seqLength, dup.size());

        // range
        Collections.sort(dup);
        Assert.assertTrue(dup.get(0) >= 1);
        Assert.assertTrue(dup.get(dup.seqLength-1) <= dup.nColors);
    }

    @Test
    public void generateTargetNoDup() throws Exception {
        // length
        Assert.assertEquals(noDup.seqLength, noDup.size());

        // range
        Collections.sort(noDup);
        Assert.assertTrue(noDup.get(0) >= 1);
        Assert.assertTrue(noDup.get(noDup.seqLength-1) <= noDup.nColors);

        // no duplicates
        Set set = new HashSet(noDup);
        Assert.assertEquals(set.size(), noDup.size());
    }
*/
}